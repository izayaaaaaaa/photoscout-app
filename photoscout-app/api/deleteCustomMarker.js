export const deleteCustomMarker = async () => {
  try {
    console.log('deleteCustomMarker runs');
  } catch (error) {
    console.error('Error deleting custom marker:', error);
    throw error;
  }
};